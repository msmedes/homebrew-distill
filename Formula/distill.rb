# typed: false
# frozen_string_literal: true

# This file was generated by GoReleaser. DO NOT EDIT.
class Distill < Formula
  desc "Turn Claude Code session transcripts into a curated model of you"
  homepage "https://github.com/msmedes/distill"
  version "0.2.10"
  license "MIT"

  on_macos do
    if Hardware::CPU.intel?
      url "https://github.com/msmedes/distill/releases/download/v0.2.10/distill_0.2.10_darwin_x86_64.tar.gz"
      sha256 "d252b2e086601efe29198581fc78dcd4095cc8936fed293fc7b46f2b11c4f23a"

      define_method(:install) do
        bin.install "distill"
      end
    end
    if Hardware::CPU.arm?
      url "https://github.com/msmedes/distill/releases/download/v0.2.10/distill_0.2.10_darwin_arm64.tar.gz"
      sha256 "c9a798779116f6508f3afd744593b02509e1675eb62dbb00ba8119db59481e65"

      define_method(:install) do
        bin.install "distill"
      end
    end
  end

  on_linux do
    if Hardware::CPU.intel? && Hardware::CPU.is_64_bit?
      url "https://github.com/msmedes/distill/releases/download/v0.2.10/distill_0.2.10_linux_x86_64.tar.gz"
      sha256 "daed6b87a77f20eeb6b2face0cf2c7e563fa8657faedaa374ad7844156f44d67"
      define_method(:install) do
        bin.install "distill"
      end
    end
    if Hardware::CPU.arm? && Hardware::CPU.is_64_bit?
      url "https://github.com/msmedes/distill/releases/download/v0.2.10/distill_0.2.10_linux_arm64.tar.gz"
      sha256 "5d91184aa4626754007af10f9642a2eef04acd884ba88c6352ee185d9116ee60"
      define_method(:install) do
        bin.install "distill"
      end
    end
  end

  def post_install
    # Restart the watcher only if it was already running, so upgrades pick
    # up the new binary. Using `launchctl kickstart` (not `brew services
    # restart`) avoids the plist-write permission error you hit when
    # brew-from-brew tries to rewrite the LaunchAgent during an upgrade.
    if OS.mac? && `brew services list 2>/dev/null`.match?(/^(msmedes\/distill\/)?distill\s+started/)
      label = "homebrew.mxcl.distill"
      plist = "#{Dir.home}/Library/LaunchAgents/#{label}.plist"
      if File.exist?(plist)
        system "launchctl", "kickstart", "-k", "gui/#{Process.uid}/#{label}"
      end
    end
  end

  def caveats
    <<~EOS
      distill calls the `claude` CLI at runtime for all LLM work — install
      Claude Code first and sign in:

        https://docs.claude.com/en/docs/claude-code

      To configure distill:

        distill install

      To run the watcher automatically with Homebrew:

        brew services start distill

      Your local state lives at ~/.distill/.
    EOS
  end

  service do
    run [opt_bin/"distill", "watch"]
    keep_alive true
    run_at_load true
    log_path var/"log/distill.log"
    error_log_path var/"log/distill.log"
  end

  test do
    system "#{bin}/distill", "help"
  end
end
